const dirs = {
    BASE: __dirname + '/',                        //корневая директория сайта
    TEMPLATES: __dirname + '/templates/',    //дирректория с шаблонами
    STATIC: __dirname + '/resources/'    //дирректория со статикой
};

exports.dirs = dirs;
